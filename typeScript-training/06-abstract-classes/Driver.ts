import { Circle } from "./Circle";
import { Rectangle } from "./Rectangle";

let myCircle = new Circle(5, 10, 20);
console.log(myCircle.getInfor());

let myRectangle = new Rectangle(0, 0, 3, 7);
console.log(myRectangle.getInfor());